{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Main Agent",
            "type": "debugpy",
            "request": "launch",
            "module": "app.agent.main_agent",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "console": "integratedTerminal"
        }
    ]
}
